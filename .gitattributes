# This .gitattributes file is recommended for Unity projects.
# It defines handling of line endings and merge strategies for Unity assets.

# Normalize line endings for text files
test/** text=auto
*.cs text
*.shader text
*.txt text

# Binary assets should be treated as such
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.tga binary
*.psd binary
*.tif binary
*.tiff binary
*.bmp binary
*.mp3 binary
*.wav binary
*.ogg binary
*.fbx binary
*.mp4 binary

# Unity asset files can use YAML merge driver
# Requires unityyamlmerge configured in .git/config
*.unity merge=unityyamlmerge eol=lf
*.prefab merge=unityyamlmerge eol=lf
*.asset merge=unityyamlmerge eol=lf
*.mat merge=unityyamlmerge eol=lf
*.anim merge=unityyamlmerge eol=lf
*.meta merge=unityyamlmerge eol=lf

